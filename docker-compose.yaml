services:
  latex:
    build:
      context: .
      dockerfile: .devcontainer/Dockerfile
    container_name: latex-glaube
    volumes:
      - .:/workspace
    working_dir: /workspace
    tty: true
    stdin_open: true
    environment:
      TEXINPUTS: "/workspaces/glaube/inc//:"
